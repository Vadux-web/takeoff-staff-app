import * as React from 'react';
import {Typography} from "@mui/material";

export const Login = () => {
    return (
        <Typography>
            Страница авторизации
        </Typography>
    );
}
