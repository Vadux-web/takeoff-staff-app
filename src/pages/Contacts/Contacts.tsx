import * as React from 'react';
import {Typography} from "@mui/material";

export const Contacts = () => {
    return (
        <Typography>
            Список контактов
        </Typography>
    );
}
